-module(evidi_analize_controller, [Req]).
-compile(export_all).

%unit_convert('GET', [AnalizaId, Sens]) ->
    %Analiza = boss_db:find(AnalizaId),
    %populate:converteste(Analiza:valoare, Analiza:factor_conversie).


